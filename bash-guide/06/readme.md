`bash -x` will put in debug mode

`set -x` To turn on

`set +x` to turn off

we can modify the PS4 variable

i.e when we have in debug mode by default the starting would be '+'
when representing the lines in the script
that can be modified by the PS4 variable

`bash -n` for syntax checking

`bash -u` for catching undefined variables

Don't try to combine `bash -nu`

variable scope in bash is same as that of environment variable scope

`shellcheck` is a nice tool
