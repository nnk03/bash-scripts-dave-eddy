# Bash Guide

Bash is a REPL ( Read Eval Print Loop ) program

1. Difference between single and double quotes in bash
2. `.sh` might mean a POSIX script which is not as featureful as bash
3. `bash -n` is a syntax checker
